import { Calculator } from "../../widgets";
import { Layout } from '../../shared/ui/layout/';

export const Home = () => {
    return (
        <Layout>
           <Calculator />
        </Layout>
    )
}